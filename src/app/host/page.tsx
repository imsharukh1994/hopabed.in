import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function HostPage() {
  return (
    <PlaceholderPage
      title="Host with Hopebed"
      description="The host dashboard, listing flow and earnings tools will live here. Become a Host is ready to connect to the host API."
      actionHref="/"
      actionLabel="Back to home"
    />
  );
}

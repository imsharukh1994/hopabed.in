import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function BookingsPage() {
  return (
    <PlaceholderPage
      title="My bookings"
      description="Your upcoming and past stays will appear here after accounts and bookings are connected."
      actionHref="/stays"
      actionLabel="Browse stays"
    />
  );
}

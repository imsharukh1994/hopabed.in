import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function OffersPage() {
  return (
    <PlaceholderPage
      title="Offers"
      description="Partner offers and seasonal deals will appear here once live inventory is connected."
      actionHref="/stays"
      actionLabel="Browse stays"
    />
  );
}

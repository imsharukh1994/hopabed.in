import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ExperiencesPage() {
  return (
    <PlaceholderPage
      title="Experiences"
      description="Hopebed Experiences will launch with verified stays. This page is a frontend placeholder."
      actionHref="/stays"
      actionLabel="Browse stays"
    />
  );
}
